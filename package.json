{
  "version": "1.0.0",
  "keywords": [
    "textlintrule"
  ],
  "main": "lib/index.js",
  "files": [
    "lib/",
    "src/"
  ],
  "scripts": {
    "test": "textlint-scripts test",
    "build": "textlint-scripts build",
    "prepublish": "npm run --if-present build",
    "watch": "textlint-scripts build --watch"
  },
  "devDependencies": {
    "@textlint/types": "^1.5.5",
    "@types/node": "^12.20.13",
    "textlint-scripts": "^12.0.0",
    "textlint-tester": "^5.3.5",
    "ts-node": "^10.0.0",
    "typescript": "^3.6.4"
  }
}

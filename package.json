{
  "version": "1.0.0",
  "keywords": [
    "textlintrule"
  ],
  "main": "lib/index.js",
  "files": [
    "lib/",
    "src/"
  ],
  "scripts": {
    "test": "textlint-scripts test",
    "build": "textlint-scripts build",
    "prepublish": "npm run --if-present build",
    "watch": "textlint-scripts build --watch"
  },
  "devDependencies": {
    "@textlint/types": "^14.8.1",
    "@types/node": "^20.19.0",
    "textlint-scripts": "^14.8.1",
    "textlint-tester": "^14.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
